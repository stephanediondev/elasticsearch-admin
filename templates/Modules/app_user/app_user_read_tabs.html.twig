<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-light{% if 'summary' == active %} active bg-dark{% endif %}" href="{{ path('app_users_read', {'user': user.id}) }}">{{ 'summary'|trans }}</a>
    </li>

    {% if is_granted('APP_USER_UPDATE', user) %}
        <li class="nav-item">
            <a class="nav-link text-light{% if 'update' == active %} active bg-dark{% endif %}" href="{{ path('app_users_update', {'user': user.id}) }}">{{ 'update'|trans }}</a>
        </li>
    {% endif %}
</ul>
