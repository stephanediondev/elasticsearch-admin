<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-{{ themeColor2 }}{% if 'summary' == active %} active bg-{{ themeColor1 }}{% endif %}" href="{{ path('app_users_read', {'user': user.id}) }}">{{ 'summary'|trans }}</a>
    </li>

    {% if is_granted('APP_USER_UPDATE', user) %}
        <li class="nav-item">
            <a class="nav-link text-{{ themeColor2 }}{% if 'update' == active %} active bg-{{ themeColor1 }}{% endif %}" href="{{ path('app_users_update', {'user': user.id}) }}">{{ 'update'|trans }}</a>
        </li>
    {% endif %}
</ul>
