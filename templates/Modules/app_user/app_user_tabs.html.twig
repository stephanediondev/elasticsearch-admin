<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-light{% if 'list' == active %} active bg-dark{% endif %}" href="{{ path('app_users') }}">{{ 'list'|trans }}</a>
    </li>

    {% if is_granted('APP_USERS_CREATE', 'global') %}
        <li class="nav-item">
            <a class="nav-link text-light{% if 'create_user' == active %} active bg-dark{% endif %}" href="{{ path('app_users_create') }}">{{ 'create_user'|trans }}</a>
        </li>
    {% endif %}
</ul>
