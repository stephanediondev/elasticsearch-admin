{% import 'Import/app_import.html.twig' as appImport %}
<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-{{ themeColor2 }}{% if 'summary' == active %} active bg-{{ themeColor1 }}{% endif %}" href="{{ path('data_streams_read', {'name': stream.name}) }}">{{ 'summary'|trans }}</a>
    </li>

    {% if is_granted('DATA_STREAM_STATS', stream) %}
        <li class="nav-item">
            <a class="nav-link text-{{ themeColor2 }}{% if 'stats' == active %} active bg-{{ themeColor1 }}{% endif %}" href="{{ path('data_streams_read_stats', {'name': stream.name}) }}">{{ 'stats'|trans }}</a>
        </li>
    {% endif %}
</ul>
