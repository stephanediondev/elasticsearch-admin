<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-{{ theme_color_2 }}{% if 'summary' == active %} active bg-{{ theme_color_1 }}{% endif %}" href="{{ path('ilm_read', {'name': policy.name}) }}">{{ 'summary'|trans }}</a>
    </li>

    {% if is_granted('ILM_POLICY_UPDATE', policy) %}
        <li class="nav-item">
            <a class="nav-link text-{{ theme_color_2 }}{% if 'update' == active %} active bg-{{ theme_color_1 }}{% endif %}" href="{{ path('ilm_update', {'name': policy.name}) }}">{{ 'update'|trans }}</a>
        </li>
    {% endif %}

    {% if is_granted('ILM_POLICY_APPLY', policy) %}
        <li class="nav-item">
            <a class="nav-link text-{{ theme_color_2 }}{% if 'apply' == active %} active bg-{{ theme_color_1 }}{% endif %}" href="{{ path('ilm_apply', {'name': policy.name}) }}">{{ 'apply'|trans }}</a>
        </li>
    {% endif %}
</ul>
