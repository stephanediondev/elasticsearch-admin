<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-light{% if 'list' == active %} active bg-dark{% endif %}" href="{{ path('index_templates_legacy') }}">{{ 'list'|trans }}</a>
    </li>

    {% if is_granted('INDEX_TEMPLATES_LEGACY_CREATE', 'global') %}
        <li class="nav-item">
            <a class="nav-link text-light{% if 'create_index_template_legacy' == active %} active bg-dark{% endif %}" href="{{ path('index_templates_legacy_create') }}">{{ 'create_index_template_legacy'|trans }}</a>
        </li>
    {% endif %}
</ul>
