{% extends 'base.html.twig' %}
{% import 'app_import.html.twig' as appImport %}

{% block head_title %}{{ indice['index'] }}{% endblock %}

{% block heading_1 %}
    {{ appImport.heading({'level': 1, 'title': 'indices'|trans, 'link': path('indices', {})}) }}
{% endblock %}

{% block heading_2 %}
    {{ appImport.heading({'level': 2, 'title': indice['index']}) }}
{% endblock %}

{% block tabs %}
    {% include 'Modules/indices/indices_read_tabs.html.twig' with {'active': 'summary'} %}
{% endblock %}

{% block main_content %}
    {% embed 'card_embed.html.twig' %}
        {% import 'app_import.html.twig' as appImport %}
        {% block content %}
            {{ appImport.heading({'level': 3, 'title': 'summary'|trans}) }}

            {% embed 'buttons_embed.html.twig' %}
                {% import 'app_import.html.twig' as appImport %}
                {% block content %}
                    {{ appImport.buttonModal({
                        'id': 'IndiceDelete' ~ indice['index'],
                        'title': 'delete'|trans,
                        'body': indice['index'],
                        'href': path('indices_delete', {'index': indice['index']}),
                    }) }}

                    {% if 'close' == indice['status'] %}
                        {{ appImport.buttonModal({
                            'id': 'IndiceOpen' ~ indice['index'],
                            'title': 'open'|trans,
                            'body': indice['index'],
                            'href': path('indices_open', {'index': indice['index']}),
                        }) }}
                    {% endif %}

                    {% if 'open' == indice['status'] %}
                        {{ appImport.buttonModal({
                            'id': 'IndiceClose' ~ indice['index'],
                            'title': 'close'|trans,
                            'body': indice['index'],
                            'href': path('indices_close', {'index': indice['index']}),
                        }) }}

                        {{ appImport.buttonModal({
                            'id': 'IndiceForceMerge' ~ indice['index'],
                            'title': 'force_merge'|trans,
                            'body': indice['index'],
                            'href': path('indices_force_merge', {'index': indice['index']}),
                        }) }}

                        {{ appImport.buttonModal({
                            'id': 'IndiceCacheClear' ~ indice['index'],
                            'title': 'cache_clear'|trans,
                            'body': indice['index'],
                            'href': path('indices_cache_clear', {'index': indice['index']}),
                        }) }}

                        {{ appImport.buttonModal({
                            'id': 'IndiceFlush' ~ indice['index'],
                            'title': 'flush'|trans,
                            'body': indice['index'],
                            'href': path('indices_flush', {'index': indice['index']}),
                        }) }}

                        {{ appImport.buttonModal({
                            'id': 'IndiceRefresh' ~ indice['index'],
                            'title': 'refresh'|trans,
                            'body': indice['index'],
                            'href': path('indices_refresh', {'index': indice['index']}),
                        }) }}
                    {% endif %}
                {% endblock %}
            {% endembed %}

            <p>
                <strong>{{ 'documents'|trans }}</strong><br>
                {{ indice['docs.count'] }}
            </p>

            <p>
                <strong>{{ 'documents_deleted'|trans }}</strong><br>
                {{ indice['docs.deleted'] }}
            </p>

            <p>
                <strong>{{ 'primary_size'|trans }}</strong><br>
                {{ indice['pri.store.size'] }}
            </p>

            <p>
                <strong>{{ 'total_size'|trans }}</strong><br>
                {{ indice['store.size'] }}
            </p>

            <p>
                <strong>{{ 'status'|trans }}</strong><br>
                {{ indice['status'] }}
            </p>

            <p>
                <strong>{{ 'health'|trans }}</strong><br>
                {{ appImport.badge({'title': indice['health'], 'context': indice['health']}) }}
            </p>
        {% endblock %}
    {% endembed %}
{% endblock %}
