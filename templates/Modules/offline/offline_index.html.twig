{% extends 'base.html.twig' %}

{% block head_title %}{{ 'offline.title'|trans }}{% endblock %}

{% block main_content %}
    <div class="jumbotron mt-3 bg-{{ themeColor4 }} text-{{ themeColor2 }} border border-{{ themeColor2 }}">
        <h1 class="display-4">{{ 'offline.title'|trans }}</h1>
        <p class="lead">{{ 'offline.note'|trans }}</p>
    </div>
{% endblock %}
