<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-{{ theme_color_2 }}{% if 'list' == active %} active bg-{{ theme_color_1 }}{% endif %}" href="{{ path('pipelines') }}">{{ 'list'|trans }}</a>
    </li>

    {% if is_granted('PIPELINES_CREATE', 'global') %}
        <li class="nav-item">
            <a class="nav-link text-{{ theme_color_2 }}{% if 'create_pipeline' == active %} active bg-{{ theme_color_1 }}{% endif %}" href="{{ path('pipelines_create') }}">{{ 'create_pipeline'|trans }}</a>
        </li>
    {% endif %}
</ul>
