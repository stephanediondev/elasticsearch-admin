<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-{{ themeColor2 }}{% if 'summary' == active %} active bg-{{ themeColor1 }}{% endif %}" href="{{ path('elasticsearch_roles_read', {'role': role.name}) }}">{{ 'summary'|trans }}</a>
    </li>

    {% if is_granted('ELASTICSEARCH_ROLE_UPDATE', role) %}
        <li class="nav-item">
            <a class="nav-link text-{{ themeColor2 }}{% if 'update' == active %} active bg-{{ themeColor1 }}{% endif %}" href="{{ path('elasticsearch_roles_update', {'role': role.name}) }}">{{ 'update'|trans }}</a>
        </li>
    {% endif %}
</ul>
