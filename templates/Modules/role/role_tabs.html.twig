<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-light{% if 'list' == active %} active bg-dark{% endif %}" href="{{ path('elasticsearch_roles') }}">{{ 'list'|trans }}</a>
    </li>

    {% if is_granted('ELASTICSEARCH_ROLES_CREATE', 'global') %}
        <li class="nav-item">
            <a class="nav-link text-light{% if 'create_role' == active %} active bg-dark{% endif %}" href="{{ path('elasticsearch_roles_create') }}">{{ 'create_role'|trans }}</a>
        </li>
    {% endif %}
</ul>
