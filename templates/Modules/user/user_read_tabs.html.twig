<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-light{% if 'summary' == active %} active bg-dark{% endif %}" href="{{ path('elasticsearch_users_read', {'user': user.name}) }}">{{ 'summary'|trans }}</a>
    </li>

    {% if is_granted('ELASTICSEARCH_USER_UPDATE', user) %}
        <li class="nav-item">
            <a class="nav-link text-light{% if 'update' == active %} active bg-dark{% endif %}" href="{{ path('elasticsearch_users_update', {'user': user.name}) }}">{{ 'update'|trans }}</a>
        </li>
    {% endif %}
</ul>
