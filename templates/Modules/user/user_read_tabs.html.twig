<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-{{ theme_color_2 }}{% if 'summary' == active %} active bg-{{ theme_color_1 }}{% endif %}" href="{{ path('elasticsearch_users_read', {'user': user.name}) }}">{{ 'summary'|trans }}</a>
    </li>

    {% if is_granted('ELASTICSEARCH_USER_UPDATE', user) %}
        <li class="nav-item">
            <a class="nav-link text-{{ theme_color_2 }}{% if 'update' == active %} active bg-{{ theme_color_1 }}{% endif %}" href="{{ path('elasticsearch_users_update', {'user': user.name}) }}">{{ 'update'|trans }}</a>
        </li>
    {% endif %}
</ul>
