<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link text-{{ themeColor2 }}{% if 'list' == active %} active bg-{{ themeColor1 }}{% endif %}" href="{{ path('elasticsearch_users') }}">{{ 'list'|trans }}</a>
    </li>

    {% if is_granted('ELASTICSEARCH_USERS_CREATE', 'global') %}
        <li class="nav-item">
            <a class="nav-link text-{{ themeColor2 }}{% if 'create_user' == active %} active bg-{{ themeColor1 }}{% endif %}" href="{{ path('elasticsearch_users_create') }}">{{ 'create_user'|trans }}</a>
        </li>
    {% endif %}
</ul>
